name: Hello
run-name: Run by ${{ github.actor}}
on: push
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello"
  share:
    runs-on: ubuntu-latest
    steps:
      - run: export RESULT="hello"
      - run: echo "${RESULT}"
